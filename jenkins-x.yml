buildPack:  maven-java11
pipelineConfig:
  agent:
    image: caladreas/builder-maven-graalvm-java11:0.0.4
  pipelines:
    overrides:
      - pipeline: release
        stage: build
        name: mvn-deploy
        containerOptions:
          env:
            - name: _JAVA_OPTIONS
              value: >-
                -XX:+UnlockExperimentalVMOptions -XX:+UseJVMCICompiler
                -XX:MinRAMPercentage=50 -XX:MaxRAMPercentage=70
                -Xms2g -Xmx2g -Xss10m
          resources:
            requests:
              cpu: "1"
              memory: 3Gi
            limits:
              cpu: "1"
              memory: 3Gi
        steps:
          - name: mvn-deploy
            # command: mvn -Pnative clean package -e --show-version
            command: mvn -Pnative clean package -e --show-version
            image: caladreas/builder-maven-graalvm-java11:0.0.4
            env:
              - name: _JAVA_OPTIONS
                value: >-
                  -XX:+UnlockExperimentalVMOptions -XX:+UseJVMCICompiler
                  -XX:MinRAMPercentage=50 -XX:MaxRAMPercentage=70
                  -Xms2g -Xmx2g -Xss10m


